/**
 * AUTO-GENERATED by scripts/gen-content.mjs — DO NOT EDIT.
 * Source of truth: api.md, mcp.md, llms.txt at the repo root.
 * Regenerate with `npm run gen` (runs automatically on deploy).
 */

/** Verbatim contents of /llms.txt. */
export const LLMS = "# ReportRoom\n> Publishing layer AI agents call directly: one API/MCP call -> a beautiful, analytics-instrumented live URL.\n## Docs\n- REST API: https://docs.reportroom.io/api\n- MCP: https://docs.reportroom.io/mcp\n## Endpoints\n- API base: https://api.reportroom.io\n- MCP: https://mcp.reportroom.io/mcp (OAuth 2.1 for claude.ai connectors, or Bearer rr_live_ key for Claude Code)\n## URL model\n- Each account gets a handle (subdomain), auto-generated at signup (an opaque token like u7k2m9qp), renameable via POST /v1/handle or the set_handle tool (e.g. to acme). Published URLs look like https://<handle>.reportroom.io/<slug>. slug is unique per account; re-publishing updates in place.\n- Email verification is required to publish: a new account is unverified and gets an API key, but POST /v1/documents (and the publish tool) return 403 email_unverified until the emailed link is clicked. GET /v1/verify?token= flips the account to verified and unlocks publishing. While unverified the reported base is https://<handle>.rrpreview.com (noindex) — you can't publish to it until verified.\n## Capabilities\n- Publish HTML or markdown -> live URL (decks + reports; charts render server-side from an ECharts option JSON block; hosted images become styled figures/galleries; optional cover_image; the workspace logo badges every doc once set via /v1/branding). Mode-A (raw HTML) opts into the design-system stylesheet by using a <main class=\"deck|report\"> shell or a var(--token) reference — otherwise it's served exactly as authored. The design system is dark-mode aware (prefers-color-scheme; .slide.dark for a forced-dark slide).\n- Whitelabel: Free-plan documents carry a \"Published with ReportRoom\" credit (fixed pill on Mode A, shell footer on Mode B); every paid plan (Pro/Team/Business) removes it.\n- Hosted images: POST /v1/images (or the upload_image tool) -> content-addressed path to reference in documents. 2 MB/file, plan caps, public or team visibility.\n- PDF export: POST /v1/documents/<slug>/export (or export_pdf tool) -> print-quality PDF. 5 credits; Idempotency-Key makes retries charge once; MCP returns a signed 1-hour download URL.\n- Data rooms (Business): gated document collections served at https://<handle>.reportroom.io/room/<slug> (access modes public/email/passcode/allowlist; NDA + expiry + CRM-webhook settings) with per-viewer analytics + lead capture — 7 MCP tools from create_data_room to get_room_analytics (max 10 rooms/workspace, 200 viewers/room).\n- Custom domains (Team/Business): POST /v1/domains -> your own hostname + DNS records (CNAME + validation TXT); GET/DELETE to manage; MCP attach_domain streams provisioning progress.\n- Analytics: GET /v1/documents/<slug>/analytics (or get_analytics) -> 7-day views + relay-ready summary.\n- Company billing: GET/PUT /v1/billing/profile (legal name, tax/VAT id, address -> printed on the Stripe invoice; PUT is owner-only) and GET /v1/billing/invoices (hosted + PDF links). Prices are exclusive of applicable taxes; no automatic tax calculation yet.\n## Plans & limits (per workspace)\n- Free: 10 docs, 10 images. Pro: 100 docs, 100 images. Team: unlimited docs, 500 images, + team-only visibility + custom domains. Business: unlimited docs, 1000 images, + data rooms. New accounts get 100 trial credits (PDF export costs 5). Over a cap -> 409 cap_reached; a gated feature off-plan -> 403 PLAN_REQUIRED/plan_required.\n## Quickstart\n- Clients: add https://mcp.reportroom.io/mcp as a custom MCP connector — Claude Code (claude mcp add --transport http), claude.ai (Settings > Connectors), ChatGPT (Developer mode > Connectors), or Codex (~/.codex/config.toml [mcp_servers]). OAuth signs in automatically, or use a Bearer rr_live_ key.\n- MCP: claude mcp add --transport http reportroom https://mcp.reportroom.io/mcp ; call create_account, verify the email, then publish\n- REST: POST /v1/signup {email} -> api_key ; verify email ; POST /v1/documents {content, content_format:\"markdown\", type:\"deck\", slug} with Authorization: Bearer -> url https://<handle>.reportroom.io/<slug>\n";

/** Rendered HTML body of the docs page (api + mcp sections). */
export const BODY = "<h2 id=\"api\">REST API reference <span class=\"pill\">REST</span></h2>\n<p>Base URL: <code>https://api.reportroom.io</code> · Versioned under <code>/v1</code>.</p>\n<h3>Quickstart</h3>\n<pre><code class=\"language-bash\"># 1. get an API key (shown once)\ncurl -sX POST https://api.reportroom.io/v1/signup \\\n  -H &#39;content-type: application/json&#39; -d &#39;{&quot;email&quot;:&quot;you@example.com&quot;}&#39;\n\n# 2. verify your email — click the link we send you. Publishing is blocked until you do.\n\n# 3. publish a markdown deck\ncurl -sX POST https://api.reportroom.io/v1/documents \\\n  -H &quot;authorization: Bearer rr_live_...&quot; -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;content&quot;:&quot;# Hello\\n\\nMy first **deck**.&quot;,&quot;content_format&quot;:&quot;markdown&quot;,&quot;type&quot;:&quot;deck&quot;,&quot;slug&quot;:&quot;hello&quot;}&#39;\n</code></pre>\n<h3>Authentication</h3>\n<p>Most endpoints require an API key as a bearer token:</p>\n<pre><code>Authorization: Bearer rr_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\n</code></pre>\n<p>Get one from <code>POST /v1/signup</code> (or the MCP <code>create_account</code> tool). Keys are shown once — store them securely. Keys look like <code>rr_live_</code> + 32 hex.</p>\n<p><strong>Verify your email before publishing.</strong> A new account receives a key immediately but is <code>unverified</code>; publishing stays blocked (<code>403 email_unverified</code>) until you click the link we email you. See <a href=\"#url-model--trust-tiers\">URL model &amp; trust tiers</a>.</p>\n<p>Errors are JSON: <code>{ &quot;error&quot;: { &quot;code&quot;: &quot;STRING_CODE&quot;, &quot;message&quot;: &quot;what to do&quot; } }</code>. The HTTP status carries the class; the <code>code</code> string is the stable machine-readable label (<strong>match on the literal string</strong> — casing is inconsistent by design, e.g. <code>cap_reached</code> vs <code>PLAN_REQUIRED</code>). MCP tools return the <strong>same</strong> code strings inside <code>structuredContent</code> with <code>isError: true</code>. Common codes by area:</p>\n<ul>\n<li><strong>Auth / account:</strong> <code>UNAUTHENTICATED</code> (401), <code>FORBIDDEN</code> (403), <code>email_unverified</code> (403), <code>INVALID_TOKEN</code> (400), <code>INVALID_EMAIL</code>, <code>DISPOSABLE_EMAIL</code>, <code>EMAIL_EXISTS</code> (409), <code>HANDLE_REJECTED</code>, <code>USER_NOT_FOUND</code> (404).</li>\n<li><strong>Publish / documents:</strong> <code>INVALID_BODY</code>, <code>EMPTY_DOCUMENT</code>, <code>INVALID_SLUG</code>, <code>INVALID_VISIBILITY</code>, <code>INVALID_COVER_IMAGE</code>, <code>IMAGE_LIMIT</code> (&gt;20 images), <code>CONTENT_BLOCKED</code>, <code>bad_replace</code>, <code>cap_reached</code> (409, carries <code>cap</code> + <code>live[]</code>), <code>PLAN_REQUIRED</code> (403, <code>team</code> visibility), <code>DOCUMENT_NOT_FOUND</code> (404).</li>\n<li><strong>Images:</strong> <code>too_large</code> (413, &gt;2 MB), <code>unsupported_format</code> (400), <code>content_blocked</code> / <code>held_for_review</code> (422), <code>cap_reached</code> (409, carries <code>cap</code> + <code>used</code>); branding: <code>NOT_FOUND</code>, <code>NOT_BRAND</code>, <code>NOT_PUBLIC</code>.</li>\n<li><strong>Export:</strong> <code>INSUFFICIENT_CREDITS</code> (402, carries <code>required</code> + <code>balance</code>), <code>IDEMPOTENCY_CONFLICT</code> (402), <code>INVALID_IDEMPOTENCY_KEY</code> (400), <code>EXPORT_UNSUPPORTED_VISIBILITY</code> (400), <code>RENDER_FAILED</code> / <code>STORAGE_FAILED</code> (502).</li>\n<li><strong>Data rooms:</strong> <code>ROOM_NOT_FOUND</code> (404), <code>VIEWER_NOT_FOUND</code> (404), <code>ROOM_LIMIT</code> (409, ≤10 rooms), <code>VIEWER_LIMIT</code> (409, ≤200 viewers), <code>plan_required</code> / <code>PLAN_REQUIRED</code> (403, Business).</li>\n<li><strong>Custom domains:</strong> <code>INVALID_HOSTNAME</code>, <code>DOMAIN_TAKEN</code> (409), <code>DOMAIN_NOT_FOUND</code> (404), <code>PAYMENT_REQUIRED</code> / <code>plan_required</code> (403), <code>CF_ERROR</code> (502).</li>\n<li><strong>Cross-cutting:</strong> <code>RATE_LIMITED</code> (429), <code>NOT_FOUND</code> (404), <code>TOO_LARGE</code> (413), <code>NOT_CONFIGURED</code> (503).</li>\n</ul>\n<h3>URL model &amp; trust tiers</h3>\n<p>Every account gets a <strong>handle</strong> — a subdomain, auto-generated at signup (an opaque token like <code>u7k2m9qp</code>) and renameable to something friendlier like <code>acme</code> (see <code>POST /v1/handle</code>). Handles are lowercase letters/numbers/hyphens (no leading/trailing or doubled hyphens), 2–32 chars. A <code>slug</code> is unique <strong>per account</strong> (not global), 3–63 chars <code>[a-z0-9-]</code>; publishing the same slug again updates in place. Verified accounts&#39; documents live at <code>https://&lt;handle&gt;.reportroom.io/&lt;slug&gt;</code>.</p>\n<p><strong>Email verification is required to publish.</strong> A fresh account is <code>unverified</code>: it gets an API key, but <code>POST /v1/documents</code> (and the MCP <code>publish</code> tool) return <code>403 email_unverified</code> until you click the verification link we email you. <code>GET /v1/verify?token=…</code> flips the account to <code>verified</code> and unlocks publishing. While unverified, <code>GET /v1/handle</code> reports a preview base on <code>rrpreview.com</code> (noindex) — this is the base your links <em>will</em> use once verified; you can&#39;t publish to it beforehand. Verification links are valid 24h; request a fresh one from your dashboard if it lapses.</p>\n<h3>Plans &amp; limits</h3>\n<p>Limits are per <strong>workspace</strong> (org). Hitting a cap returns <code>409 cap_reached</code> (documents/images) or <code>403 PLAN_REQUIRED</code>/<code>plan_required</code>/<code>PAYMENT_REQUIRED</code> (gated features). Pricing lives at <a href=\"https://reportroom.io\">reportroom.io</a>; the API only enforces the limits below.</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Plan</th>\n<th>Live documents</th>\n<th>Images</th>\n<th><code>team</code> visibility</th>\n<th>Custom domains</th>\n<th>Data rooms</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Free</td>\n<td>10</td>\n<td>10</td>\n<td>—</td>\n<td>—</td>\n<td>—</td>\n</tr>\n<tr>\n<td>Pro</td>\n<td>100</td>\n<td>100</td>\n<td>—</td>\n<td>—</td>\n<td>—</td>\n</tr>\n<tr>\n<td>Team</td>\n<td>unlimited</td>\n<td>500</td>\n<td>✓</td>\n<td>✓</td>\n<td>—</td>\n</tr>\n<tr>\n<td>Business</td>\n<td>unlimited</td>\n<td>1000</td>\n<td>✓</td>\n<td>✓</td>\n<td>✓</td>\n</tr>\n</tbody></table></div>\n<p>New accounts get <strong>100 trial credits</strong> (used by credit-metered actions like <a href=\"#post-v1documentsslugexport\">PDF export</a>, 5 credits each). Team is billed per seat; Business bundles 3 seats. <strong>Whitelabel</strong> is a paid perk: Free-plan documents carry a &quot;Published with ReportRoom&quot; credit, every paid plan removes it.</p>\n<hr>\n<h3><span class=\"method\">POST</span> /v1/signup</h3>\n<p>Create a free account and receive an API key (shown once). Rate-limited per IP.</p>\n<pre><code class=\"language-json\">Request:  { &quot;email&quot;: &quot;you@example.com&quot;, &quot;name&quot;: &quot;Optional&quot; }\nResponse: { &quot;data&quot;: { &quot;user_id&quot;: &quot;usr_…&quot;, &quot;org_id&quot;: &quot;org_…&quot;,\n                      &quot;api_key&quot;: &quot;rr_live_…&quot;, &quot;tier&quot;: &quot;unverified&quot;, &quot;message&quot;: &quot;…&quot; } }\n</code></pre>\n<p>A verification email is sent to the address. The account starts <code>unverified</code> — <strong>you must verify before publishing</strong> (see above). Rate-limited to 5/hour per IP.</p>\n<h3><span class=\"method\">GET</span> /v1/verify?token=…</h3>\n<p>Consumes the emailed verification token and upgrades the account to <code>verified</code>, which <strong>unlocks publishing</strong>. Any documents already on the preview domain migrate to <code>&lt;handle&gt;.reportroom.io</code> (old preview links 301-redirect); in the normal verify-then-publish flow you have none yet, so <code>migrated</code> is empty. Rate-limited 30/hour per IP. Returns <code>400 INVALID_TOKEN</code> if the token is missing, invalid, or expired. The link opens a human-friendly HTML page in a browser (<code>Accept: text/html</code>) and returns JSON otherwise.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;tier&quot;: &quot;verified&quot;, &quot;migrated&quot;: [], &quot;message&quot;: &quot;…&quot; } }\n</code></pre>\n<h3><span class=\"method\">POST</span> /v1/documents</h3>\n<p>Publish or update a document. <strong>Idempotent on <code>slug</code></strong> — reuse a slug to update in place. Requires auth. Provide <strong>either</strong> <code>html</code> (Mode A) <strong>or</strong> <code>content</code> + <code>content_format</code> + <code>type</code> (Mode B).</p>\n<pre><code class=\"language-json\">Request (Mode A): { &quot;html&quot;: &quot;&lt;!doctype html&gt;…&quot;, &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;title&quot;: &quot;Acme&quot; }\nRequest (Mode B): { &quot;content&quot;: &quot;# Title\\n\\nBody\\n\\n---\\n\\n## Slide 2&quot;,\n                    &quot;content_format&quot;: &quot;markdown&quot;, &quot;type&quot;: &quot;deck&quot;, &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;theme&quot;: &quot;vibrant&quot; }\nResponse: { &quot;data&quot;: { &quot;url&quot;: &quot;https://acme.reportroom.io/acme-pitch&quot;, &quot;documentId&quot;: &quot;doc_…&quot;,\n                      &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;version&quot;: 1,\n                      &quot;chartsRendered&quot;: 1, &quot;chartErrors&quot;: [],\n                      &quot;imagesRendered&quot;: 2, &quot;imageErrors&quot;: [],\n                      &quot;artifactKey&quot;: &quot;…&quot;, &quot;visibility&quot;: &quot;public&quot;, &quot;status&quot;: &quot;live&quot;,\n                      &quot;scan&quot;: { &quot;verdict&quot;: &quot;clean&quot;, &quot;score&quot;: 0, &quot;reasons&quot;: [] },\n                      &quot;removalsCount&quot;: 0, &quot;message&quot;: &quot;…&quot; } }\n</code></pre>\n<blockquote>\n<p>The <strong>MCP <code>publish</code></strong> tool returns the same data as <code>structuredContent</code> but in snake_case (<code>document_id</code>, <code>charts_rendered</code>, <code>images_rendered</code>, …) and omits <code>artifactKey</code>, <code>visibility</code>, <code>scan</code>, and <code>removalsCount</code>. There&#39;s no <code>content_format</code> on the MCP tool — <code>content</code> is always treated as markdown.</p>\n</blockquote>\n<ul>\n<li><strong>Requires a verified email</strong> — returns <code>403 email_unverified</code> otherwise (see <a href=\"#url-model--trust-tiers\">trust tiers</a>).</li>\n<li>Mode A HTML is sanitized (scripts stripped; call <code>get_design_system</code> first for on-brand output).</li>\n<li><strong>Design-system auto-injection (Mode A):</strong> if your HTML <em>opts in</em> — a root <code>&lt;main class=&quot;deck&quot;&gt;</code>/<code>&lt;main class=&quot;report&quot;&gt;</code> shell, or any reference to a ReportRoom token (<code>var(--…)</code>) — publish prepends the design-system stylesheet + webfont at the top of <code>&lt;head&gt;</code> (your own CSS comes later and always wins). HTML that opts into neither is served <strong>exactly as authored</strong> — the sheet&#39;s global reset is never forced on it. Idempotent: HTML that already carries the theme is left untouched.</li>\n<li>Rich charts: embed <code>&lt;script type=&quot;application/json&quot; data-qd-chart&gt;{…ECharts option…}&lt;/script&gt;</code> — rendered to static SVG at publish.</li>\n<li><strong>Images</strong>: reference hosted images (see <a href=\"#post-v1images\">POST /v1/images</a>) by their <code>path</code>. An image-only paragraph renders as a styled figure (an adjacent <em>italic</em> line becomes its caption); consecutive image-only paragraphs become a responsive gallery grid. Up to 20 images per document.</li>\n<li>Optional <code>cover_image</code> (a <code>/v1/images</code> <code>path</code> or an https URL): full-bleed cover behind a deck&#39;s first slide, or a top band on a report. Counts toward the 20-image budget; a bad value is <code>400 INVALID_COVER_IMAGE</code>.</li>\n<li>If your workspace has a logo set (<a href=\"#get-v1branding\">branding</a>), each publish adds a small badge to the document automatically.</li>\n<li><strong>Free-tier attribution:</strong> documents published on the <strong>Free</strong> plan carry a small &quot;Published with ReportRoom&quot; credit (a fixed pill on Mode-A docs — injected post-sanitize so it can&#39;t be stripped — or the shell footer on Mode-B). Paid plans (Pro, Team, Business) are whitelabel — no credit.</li>\n<li><code>slug</code> optional (auto-generated if omitted). Reserved slugs (<code>api</code>, <code>app</code>, <code>admin</code>, <code>mcp</code>, <code>dashboard</code>, …) are rejected.</li>\n<li>Optional <code>replace_slug</code>: retire one of your live documents to free a plan slot for this publish. Optional <code>visibility</code>: <code>public</code> (default) or <code>team</code> (members-only; needs an active Team or Business plan, else <code>403 PLAN_REQUIRED</code>).</li>\n<li>At the plan&#39;s document cap, publishing a <em>new</em> slug returns <code>409 cap_reached</code> (the response lists your live docs) — reuse a slug or pass <code>replace_slug</code>.</li>\n<li>Rate limit: 120/hour.</li>\n</ul>\n<h3><span class=\"method\">GET</span> /v1/documents</h3>\n<p>List the account&#39;s published documents. <code>?limit=</code> (default 20). Requires auth.</p>\n<h3><span class=\"method\">POST</span> /v1/documents/{slug}/unpublish</h3>\n<p>Retire one of your live documents: its URL starts returning <code>410 Gone</code> and the plan slot is freed. Idempotent. Requires auth. Returns <code>404 DOCUMENT_NOT_FOUND</code> if you don&#39;t own a live doc with that slug.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;status&quot;: &quot;unpublished&quot; } }\n</code></pre>\n<h3><span class=\"method\">POST</span> /v1/documents/{slug}/republish</h3>\n<p>Bring a previously-unpublished document back live. Re-checks the email gate and plan quota exactly like <code>publish</code> (so it can return <code>403 email_unverified</code> or <code>409 cap_reached</code>). Requires auth. <code>404 DOCUMENT_NOT_FOUND</code> if no unpublished doc with that slug is yours.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;status&quot;: &quot;live&quot; } }\n</code></pre>\n<h3><span class=\"method\">POST</span> /v1/documents/{slug}/export</h3>\n<p>Render one of your <strong>live</strong> documents to a print-quality PDF (A4, backgrounds included) and return the bytes (<code>content-type: application/pdf</code>). Requires auth. <strong>Costs 5 credits</strong>, charged only when the render succeeds — a failed render is never charged. Public/unlisted documents only for now (<code>400 EXPORT_UNSUPPORTED_VISIBILITY</code> for team-gated docs).</p>\n<pre><code>curl -X POST https://api.reportroom.io/v1/documents/acme-pitch/export \\\n  -H &quot;authorization: Bearer rr_live_...&quot; \\\n  -H &quot;Idempotency-Key: my-export-1&quot; -o acme-pitch.pdf\n</code></pre>\n<ul>\n<li><code>Idempotency-Key</code> (optional, 1–64 chars): a retried export with the same key re-renders but <strong>charges exactly once</strong> per document version. Response headers <code>x-export-charged</code> and <code>x-export-credits-remaining</code> report the burn.</li>\n<li><code>402 INSUFFICIENT_CREDITS</code> (with <code>required</code> and <code>balance</code>) when the wallet can&#39;t cover it — top up from the dashboard.</li>\n<li>Rate limit: 20/hour.</li>\n</ul>\n<h3><span class=\"method\">GET</span> /v1/documents/{slug}/analytics</h3>\n<p>Per-document view stats + a ready-to-relay summary. Requires auth.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;slug&quot;: &quot;acme-pitch&quot;, &quot;url&quot;: &quot;https://acme.reportroom.io/acme-pitch&quot;,\n                      &quot;views7d&quot;: 42, &quot;byDay&quot;: [{ &quot;day&quot;: &quot;2026-07-05&quot;, &quot;views&quot;: 8 }, …],\n                      &quot;message&quot;: &quot;\\&quot;Acme\\&quot; got 42 views in the last 7 days…&quot; } }\n</code></pre>\n<h3><span class=\"method\">GET</span> /v1/handle</h3>\n<p>Returns your current handle (subdomain) and its URL base. Requires auth. <code>url_base</code> uses <code>rrpreview.com</code> while unverified.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;handle&quot;: &quot;acme&quot;, &quot;url_base&quot;: &quot;https://acme.reportroom.io&quot; } }\n</code></pre>\n<h3><span class=\"method\">POST</span> /v1/handle</h3>\n<p>Rename your subdomain. Moves all your docs to the new handle; old links redirect. Requires auth. Returns <code>400 HANDLE_REJECTED</code> if the handle is taken, invalid, or reserved.</p>\n<pre><code class=\"language-json\">Request:  { &quot;handle&quot;: &quot;acme&quot; }\nResponse: { &quot;data&quot;: { &quot;handle&quot;: &quot;acme&quot;, &quot;moved&quot;: 3, &quot;message&quot;: &quot;…&quot; } }\n</code></pre>\n<h3><span class=\"method\">POST</span> /v1/images</h3>\n<p>Upload an image to reference in your documents. Requires auth + a verified email. Body is the <strong>raw image bytes</strong> (set the content-type) or JSON <code>{ &quot;data&quot;: &quot;&lt;base64&gt;&quot;, &quot;kind&quot;?, &quot;visibility&quot;? }</code>. PNG, JPEG, WebP or GIF (magic-byte validated — no SVG), <strong>2 MB max</strong>. Identical bytes at the same visibility dedupe to one slot.</p>\n<pre><code class=\"language-json\">Response 201: { &quot;data&quot;: { &quot;id&quot;: &quot;img_…&quot;, &quot;url&quot;: &quot;https://acme.reportroom.io/_img/org_…/&lt;sha256&gt;.png&quot;,\n                          &quot;path&quot;: &quot;/_img/org_…/&lt;sha256&gt;.png&quot;, &quot;kind&quot;: &quot;doc&quot;, &quot;visibility&quot;: &quot;public&quot;,\n                          &quot;sha256&quot;: &quot;…&quot;, &quot;bytes&quot;: 12345, &quot;content_type&quot;: &quot;image/png&quot;, &quot;deduped&quot;: false } }\n</code></pre>\n<ul>\n<li>Query/body options: <code>kind=brand</code> (a logo asset — doesn&#39;t count against any per-document budget) · <code>visibility=team</code> (serves only to signed-in workspace members; needs Team/Business, else <code>403 PLAN_REQUIRED</code>).</li>\n<li>Reference the host-agnostic <code>path</code> in your HTML/markdown — it survives handle renames.</li>\n<li>Plan caps (total images per workspace): Free 10 · Pro 100 · Team 500 · Business 1000. At cap: <code>409 cap_reached</code> with <code>cap</code> and <code>used</code>.</li>\n<li>Rate limit: 120/hour.</li>\n</ul>\n<h3><span class=\"method\">GET</span> /v1/images</h3>\n<p>List your images (newest first) with usage: <code>{ &quot;data&quot;: { &quot;images&quot;: […], &quot;used&quot;: 7, &quot;cap&quot;: 100 } }</code>. Requires auth.</p>\n<h3><span class=\"method\">DELETE</span> /v1/images/{id}</h3>\n<p>Delete an image — frees its quota slot immediately and stops its URLs serving (public image URLs may stay cached up to 1h). Requires auth. <code>404 NOT_FOUND</code> if it isn&#39;t yours.</p>\n<h3><span class=\"method\">GET</span> /v1/branding · PUT /v1/branding · DELETE /v1/branding</h3>\n<p>Your workspace logo. Once set, <strong>every document you publish afterwards</strong> carries a small logo badge (changes reach each doc on its next publish). Requires auth; <code>PUT</code>/<code>DELETE</code> need the owner or an admin role.</p>\n<pre><code class=\"language-json\">PUT request:  { &quot;image_id&quot;: &quot;img_…&quot; }   // must be YOUR image with kind=brand and visibility=public\nGET response: { &quot;data&quot;: { &quot;logo&quot;: { &quot;image_id&quot;: &quot;img_…&quot;, &quot;path&quot;: &quot;/_img/…&quot;, &quot;url&quot;: &quot;https://…&quot; } } }\n</code></pre>\n<p><code>PUT</code> validation errors are distinct: <code>404 NOT_FOUND</code> (not your image / deleted), <code>400 NOT_BRAND</code> (upload it with <code>kind=brand</code>), <code>400 NOT_PUBLIC</code> (a team-visibility image would break on public documents). <code>DELETE</code> clears the logo (idempotent).</p>\n<h3><span class=\"method\">GET</span> /v1/account</h3>\n<p>Account status: <code>handle</code>, <code>tier</code>, <code>url_base</code>, <code>org_kind</code> (<code>personal</code>/<code>team</code>), your <code>role</code> in a team workspace, and <code>scopes</code>. Requires auth.</p>\n<p>Account <strong>deletion</strong> is not agent-reachable: <code>DELETE /v1/account</code> exists but accepts <strong>only a dashboard session</strong> (guarded by typed-email confirmation). An API key or MCP token is rejected <code>403 FORBIDDEN</code>, so a leaked credential can never destroy the account that owns it.</p>\n<h3>Data rooms (Business)</h3>\n<p>A <strong>data room</strong> is a named, access-controlled bundle of your documents shared with identified external viewers at one link — for deal/diligence workflows with per-viewer engagement tracking. <strong>Business plan only</strong>, owner/admin role. Documents stay first-class: adding one to a room never unpublishes it, and a document that&#39;s in a live room is blocked at its own standalone URL so the gate can&#39;t be bypassed. The same operations are first-class <a href=\"./mcp.md#tools\">MCP tools</a> — most agents drive rooms from there.</p>\n<p>Rooms serve on your handle host: <code>https://&lt;handle&gt;.reportroom.io/room/&lt;slug&gt;</code> (and <code>/room/&lt;slug&gt;/&lt;doc-slug&gt;</code> per document). Limits: <strong>10 live rooms per workspace, 200 viewers per room.</strong></p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Endpoint</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>POST /v1/rooms</code></td>\n<td>Create a room: <code>{ name, slug?, access_mode?, passcode?, settings? }</code> → <code>{ data: { room } }</code></td>\n</tr>\n<tr>\n<td><code>GET /v1/rooms</code></td>\n<td>List your rooms</td>\n</tr>\n<tr>\n<td><code>GET /v1/rooms/{id}</code></td>\n<td>One room + its <code>documents</code> and <code>viewers</code></td>\n</tr>\n<tr>\n<td><code>PATCH /v1/rooms/{id}</code></td>\n<td>Update <code>access_mode</code>, <code>passcode</code> (string sets/rotates, <code>null</code> clears), <code>settings</code>, <code>status</code> (<code>live</code>/<code>archived</code>)</td>\n</tr>\n<tr>\n<td><code>PUT /v1/rooms/{id}/documents</code></td>\n<td>Ordered replace: <code>{ document_ids: [...] }</code> → <code>{ data: { documents } }</code></td>\n</tr>\n<tr>\n<td><code>POST /v1/rooms/{id}/viewers</code></td>\n<td>Grant a viewer: <code>{ email }</code> → <code>{ data: { viewer, invite_token } }</code> (<strong><code>invite_token</code> returned once</strong>)</td>\n</tr>\n<tr>\n<td><code>GET /v1/rooms/{id}/viewers</code></td>\n<td>List viewers + status</td>\n</tr>\n<tr>\n<td><code>DELETE /v1/rooms/{id}/viewers/{viewerId}</code></td>\n<td>Revoke a viewer — kills their live sessions immediately</td>\n</tr>\n<tr>\n<td><code>GET /v1/rooms/{id}/analytics</code></td>\n<td>Per-viewer engagement (see below)</td>\n</tr>\n</tbody></table></div>\n<p><strong>Access modes</strong> (<code>access_mode</code>): <code>public</code> (no gate), <code>email</code> (default — anyone may request a magic link), <code>passcode</code> (a shared passcode; provide <code>passcode</code> on create), <code>allowlist</code> (only pre-granted viewers get a link).</p>\n<p><strong><code>settings</code></strong> (JSON, fully replaced on update): <code>nda_text</code> (click-through NDA shown before the room renders; editing it re-prompts everyone), <code>expires_at</code> (epoch ms — past it the room returns <code>410</code> for everyone), <code>hide_branding</code> (whitelabel the viewer page), <code>crm_webhook_url</code> (https — POSTs a <code>{ type: &quot;room.lead&quot;, email, roomId, … }</code> lead when a viewer requests access). <em>(<code>allow_download</code> and <code>watermark</code> are accepted for forward-compat but not yet enforced — don&#39;t rely on them.)</em></p>\n<p><strong>Analytics</strong> (<code>GET /v1/rooms/{id}/analytics</code>): rows of <code>{ viewer_id, document_id, opens, events, dwell_ms }</code>, most-opened first. <code>email</code>/<code>allowlist</code> viewers are identity-linked; <code>public</code>/<code>passcode</code> entries are anonymous (<code>anon_…</code>). Backed by Cloudflare Analytics Engine — returns an honest empty result when analytics isn&#39;t configured.</p>\n<h3>Custom domains (Team/Business)</h3>\n<p>Serve your documents from your own hostname (e.g. <code>reports.acme.com</code>). <strong>Team or Business plan</strong>, owner/admin role.</p>\n<ul>\n<li><strong><code>POST /v1/domains</code></strong> — <code>{ &quot;hostname&quot;: &quot;reports.acme.com&quot; }</code> → <code>{ data: { hostname, status, dns, message } }</code>. <code>dns</code> is the records to create at your DNS provider: a <strong>CNAME</strong> pointing the hostname at ReportRoom&#39;s target, plus any <strong>TXT</strong> records for hostname-ownership and TLS-certificate validation. Create them, then poll status.</li>\n<li><strong><code>GET /v1/domains</code></strong> — <code>{ data: { domains: [{ hostname, status, dns? }] } }</code>. <code>status</code> moves <code>pending</code> → <code>pending_validation</code> → <code>active</code>; <code>dns</code> is included until it&#39;s <code>active</code>. Once <code>active</code>, your documents&#39; <code>url</code> switches to the custom host.</li>\n<li><strong><code>DELETE /v1/domains/{hostname}</code></strong> — detaches → <code>{ data: { deleted: &quot;reports.acme.com&quot; } }</code>.</li>\n</ul>\n<p>Errors: <code>403 PAYMENT_REQUIRED</code> (free) / <code>403 plan_required</code> (Pro — upgrade to Team/Business), <code>400 INVALID_HOSTNAME</code>, <code>409 DOMAIN_TAKEN</code>, <code>404 DOMAIN_NOT_FOUND</code>, <code>502 CF_ERROR</code>, <code>503 NOT_CONFIGURED</code>. Rate limit: 10/hour per workspace.</p>\n<p>Agents can use the streaming <strong><code>attach_domain</code></strong> MCP tool instead — with an <code>Accept: text/event-stream</code> request it emits <code>notifications/progress</code> frames as provisioning advances and returns the DNS records for the human to create.</p>\n<h3>Billing for companies</h3>\n<p>Buying as a company? Checkout collects your legal entity name, billing address and tax/VAT\nnumber, and Stripe issues an invoice carrying them. You can also edit the details any time\nfrom the dashboard (Settings → Plan &amp; billing → <strong>Company &amp; invoices</strong>) or over the API.</p>\n<p>Prices are quoted <strong>exclusive of any applicable taxes</strong>. A VAT ID you provide appears on the\ninvoice as your identity; we do not currently apply automatic tax calculation or reverse\ncharge.</p>\n<h4>GET /v1/billing/profile</h4>\n<p>Your workspace&#39;s company billing details. Readable by any workspace member. Requires auth.</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;profile&quot;: { &quot;companyName&quot;: &quot;Acme GmbH&quot;, &quot;taxId&quot;: &quot;DE123456789&quot;,\n                                   &quot;taxIdType&quot;: &quot;eu_vat&quot;, &quot;country&quot;: &quot;DE&quot;, &quot;line1&quot;: &quot;Hauptstr. 1&quot;,\n                                   &quot;city&quot;: &quot;Berlin&quot;, &quot;postalCode&quot;: &quot;10115&quot;,\n                                   &quot;billingEmail&quot;: &quot;ap@acme.com&quot;, &quot;reference&quot;: &quot;PO-2026-0042&quot; },\n                      &quot;invoice_ready&quot;: true } }\n</code></pre>\n<p><code>invoice_ready</code> is <code>true</code> once there&#39;s enough for a bookable invoice (name + full address).</p>\n<h4>PUT /v1/billing/profile</h4>\n<p>Save the details and push them to our payment processor, which is what prints them on your\ninvoice. <strong>Workspace owner only.</strong> Send any subset; an empty string clears a field.</p>\n<pre><code class=\"language-json\">Request: { &quot;company_name&quot;: &quot;Acme GmbH&quot;, &quot;tax_id&quot;: &quot;DE123456789&quot;, &quot;tax_id_type&quot;: &quot;eu_vat&quot;,\n           &quot;country&quot;: &quot;DE&quot;, &quot;line1&quot;: &quot;Hauptstr. 1&quot;, &quot;city&quot;: &quot;Berlin&quot;, &quot;postal_code&quot;: &quot;10115&quot;,\n           &quot;billing_email&quot;: &quot;ap@acme.com&quot;, &quot;reference&quot;: &quot;PO-2026-0042&quot; }\n</code></pre>\n<ul>\n<li><code>tax_id_type</code> is one of <code>eu_vat</code>, <code>gb_vat</code>, <code>ch_vat</code>, <code>no_vat</code>, <code>us_ein</code>, <code>ca_bn</code>, <code>au_abn</code>, <code>nz_gst</code>, <code>ae_trn</code>, <code>sg_gst</code>, <code>in_gst</code>, <code>jp_cn</code>, <code>br_cnpj</code>, <code>mx_rfc</code>. Send <code>tax_id</code> and <code>tax_id_type</code> <strong>together</strong> (or neither) — <code>400 TAX_ID_INCOMPLETE</code> otherwise.</li>\n<li><code>country</code> is a 2-letter ISO code. A tax number our processor rejects outright returns <code>400 TAX_ID_REJECTED</code> with the reason — the rest of your details are still saved.</li>\n<li><strong>A saved number is not yet a verified one.</strong> Tax IDs are checked with the tax authority <em>asynchronously</em>, so a <code>200</code> means &quot;stored&quot;, not &quot;valid&quot;. The response and <code>GET /v1/billing/profile</code> carry <code>taxIdVerification</code>: <code>pending</code> (being checked), <code>verified</code>, <code>unverified</code> (the authority could not confirm it — it will still print on your invoices, so check for typos), or <code>unavailable</code> (no automatic check exists for that id type). Your workspace&#39;s Company &amp; invoices page shows the same state.</li>\n<li><code>data.synced</code> is <code>false</code> when your workspace has no payment account yet: the details are stored and applied at your first subscription.</li>\n</ul>\n<h4>GET /v1/billing/invoices</h4>\n<p>Your invoice history (subscriptions and credit top-ups), newest first, with links to the\nhosted invoice and its PDF. <strong>Workspace owner only.</strong> <code>?limit=</code> up to 100 (default 24).</p>\n<pre><code class=\"language-json\">Response: { &quot;data&quot;: { &quot;invoices&quot;: [ { &quot;number&quot;: &quot;RR-0042&quot;, &quot;created&quot;: 1786000000000,\n                                      &quot;status&quot;: &quot;paid&quot;, &quot;total&quot;: 4900, &quot;currency&quot;: &quot;usd&quot;,\n                                      &quot;hostedUrl&quot;: &quot;https://…&quot;, &quot;pdfUrl&quot;: &quot;https://…&quot;,\n                                      &quot;reason&quot;: &quot;subscription_cycle&quot; } ] } }\n</code></pre>\n<p><code>total</code> is in the currency&#39;s minor unit (cents); <code>created</code> is milliseconds since epoch.</p>\n<h3><span class=\"method\">POST</span> /v1/lint</h3>\n<p>Pre-flight check an HTML document before publishing (missing viewport/og, stripped scripts, off-brand). No auth.</p>\n<pre><code class=\"language-json\">Request:  { &quot;html&quot;: &quot;&lt;!doctype html&gt;…&quot; }\nResponse: { &quot;data&quot;: { &quot;ok&quot;: true, &quot;issues&quot;: [ { &quot;level&quot;: &quot;warning&quot;, &quot;code&quot;: &quot;no-og-title&quot;, &quot;message&quot;: &quot;…&quot; } ] } }\n</code></pre>\n<h3><span class=\"method\">GET</span> /v1/design-system?theme=</h3>\n<p>Returns everything an agent should follow <strong>before</strong> authoring HTML. No auth. Response <code>data</code> keys: <code>theme</code>, <code>version</code>, <code>tokens</code> (CSS custom properties), <code>rules</code> (hard constraints), <code>components</code> (ready-to-paste snippets — slides, KPI cards, callouts, charts, figures, galleries, …), and <code>shells</code> (<code>{ deck, report }</code> document skeletons); a top-level <code>themes</code> lists the available themes. There is currently one theme — <strong><code>vibrant</code></strong> (Midnight Azure: ink-violet headings, azure accents on a white reading surface) — which is also the default. The design system is <strong>dark-mode aware</strong>: documents adapt to the reader&#39;s <code>prefers-color-scheme</code>, and authors can force an ink slide with <code>class=&quot;slide dark&quot;</code>.</p>\n<h3><span class=\"method\">POST</span> /v1/report-abuse</h3>\n<p>Report an abusive published page. No auth, rate-limited.</p>\n<pre><code class=\"language-json\">Request: { &quot;url&quot;: &quot;https://bad-handle.reportroom.io/bad-slug&quot;, &quot;reason&quot;: &quot;phishing&quot; }\n</code></pre>\n\n<h2 id=\"mcp\">MCP server <span class=\"pill\">MCP</span></h2>\n<p>ReportRoom ships a Model Context Protocol server so agents (Claude Code, Cursor, claude.ai, …) can publish and track documents natively.</p>\n<ul>\n<li><strong>Endpoint:</strong> <code>https://mcp.reportroom.io/mcp</code></li>\n<li><strong>Transport:</strong> Streamable HTTP (JSON-RPC over POST). Protocol versions: <code>2025-11-25</code>, <code>2025-06-18</code>, <code>2025-03-26</code>.</li>\n<li><strong>Auth:</strong> OAuth 2.1 (claude.ai and other connectors) <strong>or</strong> <code>Authorization: Bearer rr_live_…</code> (Claude Code). No key yet? Call the <code>create_account</code> tool — it returns one.</li>\n</ul>\n<h3>Connect</h3>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-bash\">claude mcp add --transport http reportroom https://mcp.reportroom.io/mcp\n# in a session:\n#   1) call create_account (email) -&gt; save the api_key\n#   2) re-add with the key:\nclaude mcp add --transport http reportroom https://mcp.reportroom.io/mcp \\\n  --header &quot;Authorization: Bearer rr_live_...&quot;\n</code></pre>\n<p><strong>claude.ai (web &amp; desktop):</strong> Settings → Connectors → <strong>Add custom connector</strong> →\npaste <code>https://mcp.reportroom.io/mcp</code> → Connect. ReportRoom is a full OAuth 2.1\nauthorization server, so the client discovers auth automatically and walks you through\nsign-in — no key to paste. (Requires a plan that allows custom connectors.)</p>\n<p><strong>ChatGPT:</strong> custom MCP connectors run in <strong>Developer mode</strong> (Plus, Pro, Team,\nEnterprise, or Edu — not Free).</p>\n<ol>\n<li>Settings → <strong>Apps &amp; Connectors</strong> → Advanced → turn on <strong>Developer mode</strong>.</li>\n<li>Settings → Connectors → <strong>Create</strong> → name it &quot;ReportRoom&quot;, set the connector URL to\n<code>https://mcp.reportroom.io/mcp</code>.</li>\n<li>Authenticate when prompted (OAuth).</li>\n</ol>\n<p><strong>Codex CLI:</strong> add a streamable-HTTP server to <code>~/.codex/config.toml</code>, then sign in:</p>\n<pre><code class=\"language-toml\">[mcp_servers.reportroom]\nurl = &quot;https://mcp.reportroom.io/mcp&quot;\n# optional — API key instead of OAuth:\n# bearer_token_env_var = &quot;REPORTROOM_KEY&quot;\n</code></pre>\n<pre><code class=\"language-bash\">codex mcp login reportroom   # OAuth sign-in\n# verify with /mcp in the Codex TUI\n</code></pre>\n<h4>OAuth endpoints (for connectors that discover auth automatically)</h4>\n<ul>\n<li><strong>Protected-resource metadata (RFC 9728):</strong> <code>GET /.well-known/oauth-protected-resource</code></li>\n<li><strong>Authorization-server metadata (RFC 8414):</strong> <code>GET /.well-known/oauth-authorization-server</code></li>\n<li><strong>Dynamic Client Registration (RFC 7591):</strong> <code>POST /oauth/register</code></li>\n<li><strong>Authorization Code + PKCE (S256):</strong> <code>GET /oauth/authorize</code> → <code>POST /oauth/token</code> (+ <code>refresh_token</code>)</li>\n</ul>\n<p>Auth-required tools answer <code>401</code> with a <code>WWW-Authenticate</code> challenge pointing at the\nmetadata above, which is what kicks off the connector&#39;s OAuth flow. Discovery and\n<code>create_account</code> stay open so agents can bootstrap with zero setup.</p>\n<h3>Tools</h3>\n<p>23 tools. Every tool returns human-readable text plus <code>structuredContent</code>; errors set <code>isError</code> with actionable guidance.</p>\n<p><strong>Author &amp; publish:</strong></p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n<th>Auth</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>create_account</code></td>\n<td>Bootstrap a free account; returns an API key (shown once)</td>\n<td>no</td>\n</tr>\n<tr>\n<td><code>get_design_system</code></td>\n<td>Design tokens + component snippets + rules — call <strong>before</strong> authoring HTML</td>\n<td>no</td>\n</tr>\n<tr>\n<td><code>list_themes</code></td>\n<td>Available design themes</td>\n<td>no</td>\n</tr>\n<tr>\n<td><code>lint_document</code></td>\n<td>Pre-flight check HTML before publish</td>\n<td>no</td>\n</tr>\n<tr>\n<td><code>publish</code></td>\n<td>Publish/update a deck or report (Mode A <code>html</code> or Mode B <code>content</code>+<code>type</code>); idempotent on <code>slug</code>; optional <code>visibility</code> (<code>team</code> = members-only, paid) and <code>cover_image</code>; returns the full live URL. <strong>Requires a verified email</strong></td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>list_documents</code></td>\n<td>List your published documents</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>unpublish</code></td>\n<td>Retire a live document: its URL returns <code>410 Gone</code> and the plan slot frees. Idempotent</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>republish</code></td>\n<td>Bring an unpublished document back live (re-checks the email gate + plan quota)</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>get_analytics</code></td>\n<td>Per-document views + a summary you can relay to the human</td>\n<td>yes</td>\n</tr>\n</tbody></table></div>\n<p><strong>Images</strong> (reference the returned <code>path</code> in your document; an image-only paragraph becomes a styled figure, consecutive ones a gallery):</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n<th>Auth</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>upload_image</code></td>\n<td>Upload base64 image bytes (PNG/JPEG/WebP/GIF, 2 MB max, plan caps; <code>kind=brand</code> for a logo, <code>visibility=team</code> for members-only)</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>list_images</code></td>\n<td>Your hosted images with used/cap</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>delete_image</code></td>\n<td>Delete by id — frees the slot immediately (public image URLs may stay cached up to 1h)</td>\n<td>yes</td>\n</tr>\n</tbody></table></div>\n<p><strong>Export:</strong></p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n<th>Auth</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>export_pdf</code></td>\n<td>Render a live document to a print-quality PDF (5 credits; <code>idempotency_key</code> makes retries charge once). Returns a <strong>signed download URL valid 1 hour</strong></td>\n<td>yes</td>\n</tr>\n</tbody></table></div>\n<p><strong>Account &amp; domains:</strong></p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n<th>Auth</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>account_status</code></td>\n<td>Tier, limits, handle, your role in a team workspace</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>set_handle</code></td>\n<td>Rename your account&#39;s subdomain (handle); moves all your docs, old links redirect</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>attach_domain</code></td>\n<td>Attach your own hostname (Team/Business): streams provisioning progress, returns the DNS records for the human to create</td>\n<td>yes</td>\n</tr>\n</tbody></table></div>\n<p><strong>Data rooms</strong> (Business — gated deal-room collections with per-viewer tracking):</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n<th>Auth</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>create_data_room</code></td>\n<td>Create a room (name, slug) to share documents behind a gate</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>add_documents_to_room</code></td>\n<td>Set which of your documents the room contains</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>set_room_access</code></td>\n<td>Configure the gate: email verification, passcode, NDA click-through, expiry, allowlist</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>grant_room_access</code> / <code>revoke_room_access</code></td>\n<td>Manage individual viewers</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>list_room_viewers</code></td>\n<td>Who has access + status</td>\n<td>yes</td>\n</tr>\n<tr>\n<td><code>get_room_analytics</code></td>\n<td>Per-viewer engagement (opens, dwell, which documents) — the &quot;who read the deck&quot; answer</td>\n<td>yes</td>\n</tr>\n</tbody></table></div>\n<h3>URLs</h3>\n<p>Each account gets a <strong>handle</strong> — a subdomain, auto-generated on signup (an opaque token like <code>u7k2m9qp</code>) and renameable any time via <code>set_handle</code> (e.g. to <code>acme</code>). Published docs live at <code>https://&lt;handle&gt;.reportroom.io/&lt;slug&gt;</code>.</p>\n<p><strong>Publishing requires a verified email.</strong> A new account gets an API key but is <code>unverified</code>; <code>publish</code> fails with <code>email_unverified</code> until the human clicks the verification link (check the inbox after <code>create_account</code>). While unverified, <code>account_status</code> reports a preview base on <code>rrpreview.com</code> (noindex) — the base your links will use once verified; you can&#39;t publish there beforehand.</p>\n<p><code>publish</code> is idempotent on <code>slug</code> (per account) and returns the full <code>url</code> in <code>structuredContent</code>.</p>\n<h3>Recommended flow</h3>\n<p><code>create_account</code> (then have the human <strong>verify their email</strong> — publishing is blocked until they do) → <code>get_design_system</code> → author self-contained HTML following the tokens → <code>upload_image</code> for any figures/logo → <code>lint_document</code> → <code>publish</code> (with <code>cover_image</code> for a hero) → later, <code>get_analytics</code> to report back who viewed it. Optionally <code>set_handle</code> once to pick a nicer subdomain (old links redirect), <code>export_pdf</code> when the human wants a file, and <code>unpublish</code>/<code>republish</code> to retire or restore a document. For deal workflows on Business: <code>create_data_room</code> → <code>add_documents_to_room</code> → <code>set_room_access</code> → share the room link → <code>get_room_analytics</code> to report per-viewer engagement.</p>\n<p>Two <code>publish</code> behaviors worth knowing (details in the <a href=\"https://docs.reportroom.io/api\">REST reference</a>): raw-HTML documents opt into the design-system stylesheet by using a <code>&lt;main class=&quot;deck&quot;&gt;</code>/<code>&lt;main class=&quot;report&quot;&gt;</code> shell or a <code>var(--token)</code> reference — otherwise they&#39;re served exactly as authored; and <strong>Free-plan</strong> documents carry a &quot;Published with ReportRoom&quot; credit that every paid plan removes.</p>";
